\documentclass[11pt, a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[charter]{mathdesign} % Elegant, readable font
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{enumitem}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{graphicx} % Required for inserting images (put this in your preamble)
\usepackage{subfig}
\usepackage{comment}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage{colortbl}


\makeatletter
\renewcommand{\maketitle}{
  \begin{flushleft}
    {\huge \textbf{\@title} \par}
    \vspace{0.5cm}
    {\large \@author \par}
    \vspace{0.2cm}
    {\@date \par}
    \vspace{0.2cm}
    \hrule
  \end{flushleft}
}
\makeatother

\title{\LARGE\textbf{Molecular dynamics: 2D Lennard-Jones Simulation} \\[-0.2em]}
\author{
    Plabon Shaha and Guillem Masdemont \\ 
    \small University of Ljubljana, Spring Semester, EMAI Cohort 25--27 
}

\begin{document}

\maketitle

\begin{enumerate}
  \item[\textbf{Implementation}] We parallelize the algorithm and present several versions that are built on top of each other:

\definecolor{v1color}{HTML}{4C72B0}
\definecolor{v2color}{HTML}{FF7F0E}
\definecolor{v3color}{HTML}{2CA02C}
\definecolor{v4color}{HTML}{D62728}
\definecolor{v5color}{HTML}{9467BD}
\definecolor{v6color}{HTML}{8C564B}
\definecolor{v7color}{HTML}{E377C2}
\definecolor{v8color}{HTML}{7F7F7F}

\newcommand{\versionbox}[4]{%
    \begin{tikzpicture}
        \node[
            draw=#1, line width=1.4pt,
            rounded corners=5pt,
            fill=#1!10,
            minimum width=3.9cm,
            minimum height=3.0cm,
            text width=3.6cm,
            align=left,
            inner sep=7pt
        ] {
            \textcolor{#1}{\textbf{\footnotesize #2}}\\[3pt]
            {\scriptsize #3}\\[4pt]
            \textcolor{#1}{\scriptsize\textbf{#4}}
        };
    \end{tikzpicture}%
}

\begin{table}[h]
\centering
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{cccc}

\versionbox{v1color}%
    {V1 — CPU baseline}%
    {\textbullet\ $O(n^2)$ nested loop\\
     \textbullet\ Each pair counted twice\\
     \textbullet\ Single CPU core}%
    {Baseline: $\approx 0.08\times$ vs V2} &

\versionbox{v2color}%
    {V2 — OpenMP CPU}%
    {\textbullet\ $O(n^2)$, unique pairs only\\
     \textbullet\ Newton's 3rd law\\
     \textbullet\ Multi-core via OpenMP}%
    {Reference: $1\times$} &

\versionbox{v3color}%
    {V3 — GPU 2D grid}%
    {\textbullet\ GPU forces, 2D grid\\
     \textbullet\ One thread per $(i,j)$\\
     \textbullet\ Per-step malloc/free}%
    {$\approx 4\times$ vs V2} &

\versionbox{v4color}%
    {V4 — GPU 1D pairs}%
    {\textbullet\ 1D kernel, unique pairs\\
     \textbullet\ Newton's 3rd law on GPU\\
     \textbullet\ Integration on CPU}%
    {$\approx 4.5\times$ vs V2} \\[8pt]

\versionbox{v5color}%
    {V5 — Fully GPU, AoS}%
    {\textbullet\ All steps on GPU\\
     \textbullet\ Single malloc/free\\
     \textbullet\ Data stays on device}%
    {$\approx 5\times$ vs V2} &

\versionbox{v6color}%
    {V6 — Fully GPU, SoA}%
    {\textbullet\ SoA layout\\
     \textbullet\ Coalesced warp access\\
     \textbullet\ $r^2$ cutoff check}%
    {$\approx 5.4\times$ vs V2} &

\versionbox{v7color}%
    {V7 — Neighbour list}%
    {\textbullet\ Verlet list, breaks $O(n^2)$\\
     \textbullet\ Forces on nearby only\\
     \textbullet\ Rebuild on drift}%
    {$\approx 7.6\times$ vs V2} &

\versionbox{v8color}%
    {V8 — 2-GPU naive}%
    {\textbullet\ Particles split across 2 GPUs\\
     \textbullet\ P2P position exchange\\
     \textbullet\ Full GPU integration}%
    {$\approx 3\times$ vs V2} \\

\end{tabular}
\caption{Summary of all versions. Speedups at $N=1000$ steps, relative to V2 (OpenMP CPU).}
\label{tab:version_grid}
\end{table}

\item[\textbf{Evaluation:}] We first analyze the performance across diferent version for different number of particles (see Figure 1), we plot logaritmic execution time scale. 

\begin{figure}[h]
    \centering
    \includegraphics[width=1.05\textwidth]{performance_variance_plot.png}
    \caption{Performance comparison of the aforementioned CPU and CUDA optimization layers.}
    \label{fig:performance_plots}
\end{figure}

As shown in class, we see that the Naive CPU and OpenMP CPU baselines (blue-orange) scale as $O(n^2)$, going from N=1000 to N=8000 increases OpenMP CPU time by roughly $128 \times$ (from $~2.5s$ to $~250s$), consistent with squaring the particle count. The GPU versions all show much shallower scaling. $gpu\_v2$ and $gpu\_v3$ (green and red) improve over CPU but still carry significant per-step transfer overhead since they allocate and free GPU memory every step, at N=8000 they reach $~8s$ (about $30\times$ faster than CPU). $gpu\_v4$ and $gpu\_v5$ (purple and brown) eliminate this overhead by keeping data resident on the GPU, bringing $N=8000$ down to $~2-3s$ and achieving roughly $80-100\times$ speedup over CPU. The neighbour list version $gpu\_v6$ (pink) is the clear winner at all particle counts, at $N=8000$ it reaches $~0.8s$, nearly $300\times$ faster than CPU, because it breaks the $O(n^2)$ wall by limiting each particle to $~40$ neighbours rather than $8000$. The 2-GPU version $gpu\_v7$ (grey) is slower than $gpu\_v6$ at all $N$, because the position exchange between devices at each step introduces synchronisation overhead that outweighs the benefit of splitting the work, at $N=1000$ and $N=2000$ the problem is simply too small to amortise that cost, and at $N=8000$ the exchange still costs more than the neighbour list saves, although more close. We believe that combining the 2 GPU version with the V7 - Neighbour list, will not improve the result due to the memory transfer overhead. One possible way could be to try CUDA constant memory for simulation parameters such as $\epsilon$, $\sigma$, and $r_\text{cut}$, which are read by every thread every step but never modified.

Now we present the performance of V5 - Fully GPU, AoS version for different block sizes: 

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\textwidth]{block_size_tuning_N4000.png}
    \caption{Block size tuning results for the V5 force kernel (N = 4000).}
    \label{fig:block_size_tuning} 
\end{figure}

The sweep shows a clear performance cliff at small block sizes — block=32 takes 0.3416 ms/step, roughly 3× slower than the optimum. This is because 32 threads is exactly one warp, giving the scheduler no room to hide memory latency: when a thread stalls waiting for a global memory read, the entire block stalls with nothing to switch to. Doubling to block=64 already halves the time to 0.1788 ms/step, and block=128 drops further to 0.1149 ms/step as latency hiding improves with more warps in flight per block. Beyond 128 the curve flattens — 256, 512, and 128 all measure within 0.0002 ms/step of each other (0.1147–0.1149), meaning the GPU is fully saturated and additional warps per block provide no further benefit. Block=1024 slightly regresses to 0.1163 ms/step, likely due to increased register pressure reducing the number of blocks that can reside simultaneously on each streaming multiprocessor. In practice, blocksizes of 128 to 512 are optimal. In this scenario, we only measured the performance on computing the force, but since force kernel dominates runtime, the chosen block size is effectively optimal for the full leapfrog step.

\end{enumerate}

\end{document}